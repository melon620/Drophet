# Drophet — DDI Tox-Predict pipeline
# Pinning is conservative: minor-version floors that match the API surface
# the scripts actually use. Tighten further once a single CI/training
# environment is canonical.

# --- Data / chemistry ---
numpy>=1.24,<2.0
pandas>=1.5,<3.0
scipy>=1.10,<2.0
rdkit>=2022.09.1

# --- Classical ML ---
scikit-learn>=1.3,<2.0
xgboost>=2.0,<3.0
shap>=0.44,<1.0

# --- Deep learning (GNN) ---
# NOTE: torch-geometric requires a torch install matching the wheel
# (CUDA/CPU). On Apple Silicon use the CPU build of torch first, then
# install torch-geometric. See README "Setup" section.
torch>=2.1,<3.0
torch-geometric>=2.4,<3.0

# --- LLM / API clients used in the data pipeline ---
openai>=1.0,<2.0
google-genai>=0.5.0

# --- NLP for adverse-event categorization ---
spacy>=3.7,<4.0
# The scispacy model `en_core_sci_sm` must be installed separately from the
# wheel URL published by AllenAI; see README.
scispacy>=0.5.4,<1.0

# --- IO / utilities ---
requests>=2.28,<3.0
python-dotenv>=1.0,<2.0
matplotlib>=3.7,<4.0
PyYAML>=6.0,<7.0
