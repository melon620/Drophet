# Environment variables
.env
.env.local
.env.*.local

# Virtual environment
.venv/
venv/
ENV/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data files (optional — force-add curated reference JSONs with `git add -f`)
*.csv
*.json

# Model artifacts: these belong in a model registry / DVC, not in git.
# Existing .pth files in this repo are tracked from earlier history; new ones
# should not be committed. Run `git rm --cached <file>.pth` to untrack.
*.pth
*.pkl
*.onnx
*.joblib

# Local logs / experiment runs
logs/
runs/
mlruns/
wandb/
*.log

# Secrets
.env
.env.*
secrets/

